
# TODO: написать тесты

async def register_tutor(tutor_chat_id: int, message_text: str, ):
    # TODO: проверить формат через regex

    # TODO: сохранить в базу

    # TODO: сохранить токен в редис с TTL 11 мин
    # await redisClient.set(possible_token, str(current_user_id))
    # await redisClient.expire(possible_token, 11 * 60)

    pass